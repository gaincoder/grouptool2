{% extends 'closed_area/base.html.twig' %}
{% block content_row %}
    <div class="container container-fluid" id="home">
        <!-- HOME -->

        <!-- ABOUT & UPDATES -->
        <div class="row-fluid" id="about">
            {{ form_start(form) }}
            <div class="span12">
                <h2 class="page-title" id="scroll_up"> {{ page_title }}</h2>
                <br><br>
                {{ form_widget(form) }}
            </div>
            <br><br>
            <button type="submit" class="btn btn-success" name="_submit">hochladen</button>
            <a href="{{ path('photoalbum_view',{'photoalbum':album.id}) }}" class="btn btn-default" name="_submit">zurück</a>
            {{ form_end(form) }}
        </div>


    </div>
{% endblock %}